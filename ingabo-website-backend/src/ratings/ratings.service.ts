import { Injectable } from '@nestjs/common'; import { InjectModel } from '@nestjs/mongoose'; import { Model } from 'mongoose'; import { CreateRatingDto } from './dto/create-rating.dto'; import { Rating, RatingDocument } from './schemas/rating.schema'; @Injectable() export class RatingsService { constructor(@InjectModel(Rating.name) private readonly ratings: Model<RatingDocument>) {} create(dto: CreateRatingDto) { return this.ratings.create(dto); } findAll() { return this.ratings.find().sort({ createdAt: -1 }).lean().exec(); } async stats() { const [result] = await this.ratings.aggregate([{ $group: { _id: null, total: { $sum: 1 }, average: { $avg: '$rating' } } }]); return result ?? { total: 0, average: 0 }; } }
